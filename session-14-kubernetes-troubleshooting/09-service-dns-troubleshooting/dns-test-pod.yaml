apiVersion: v1
kind: Pod

metadata:
  name: dns-test

spec:
  containers:
    - name: dns-test
      image: registry.k8s.io/e2e-test-images/dnsutils:1.3
      command:
        - sleep
        - "3600"